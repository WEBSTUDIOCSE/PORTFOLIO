// Total viewports of vertical scroll the journey runway presents.
// The train glides continuously across the entire runway (no per-
// viewport reset) so scrolling reads as one long, smooth ride.
export const JOURNEY_VIEWPORTS = 3;

// Train motion in world X. The train glides from X_START to X_END
// as the user scrolls from the top to the bottom of the runway.
export const TRAIN_X_START = -12;
export const TRAIN_X_END   = +12;
