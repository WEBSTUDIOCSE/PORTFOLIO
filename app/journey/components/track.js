// Shared track-geometry constants for the straight rail line.
// Track runs along world +X axis, centred on origin, at Y = 0, Z = 0.

export const TRACK_LENGTH        = 100;       // total X span (-50..+50) — wide enough to overflow ultra-wide viewports
export const RAIL_GAUGE          = 1.676;     // Indian broad gauge (m)
export const RAIL_RADIUS         = 0.04;      // tube radius
export const RAIL_HEIGHT_OFFSET  = 0.18;      // rail top above ground
export const SLEEPER_SPACING     = 0.42;      // metres between sleepers
